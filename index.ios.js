import React, {
  Component
} from 'react';
import {
  AppRegistry,
  Text,
} from 'react-native';

import Application from './src/App';

AppRegistry.registerComponent('ReactDemo', () => Application);
